import { ErrorMsgFormsDirective } from './error-msg-forms.directive';

describe('ErrorMsgFormsDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorMsgFormsDirective();
    expect(directive).toBeTruthy();
  });
});
