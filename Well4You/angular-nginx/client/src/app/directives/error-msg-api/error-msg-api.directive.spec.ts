import { ErrorMsgApiDirective } from './error-msg-api.directive';

describe('ErrorMsgApiDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorMsgApiDirective();
    expect(directive).toBeTruthy();
  });
});
